import 'package:flutter/cupertino.dart';

class cordlessTwo extends StatelessWidget {
  const cordlessTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Hey"));
  }
}
